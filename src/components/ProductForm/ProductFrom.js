export const FormProps = {
    formData: {
        type: Object,
        required: true,
    },
    submitText: {
        type: String,
        required: true,
    },
    loading: {
        type: Boolean,
        required: true,
    },
};
export const services = ["7天无理由", "30天报价", "2年只换不修"];
